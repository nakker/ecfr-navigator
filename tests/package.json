{
  "name": "ecfr-analyzer-tests",
  "version": "1.0.0",
  "description": "Test suite for eCFR Analyzer",
  "scripts": {
    "test": "jest --config=jest.config.js",
    "test:frontend": "jest --config=jest.config.frontend.js",
    "test:watch": "jest --config=jest.config.js --watch",
    "test:coverage": "jest --config=jest.config.js --coverage",
    "test:ci": "jest --config=jest.config.js --ci --coverage --maxWorkers=2",
    "test:unit": "jest --config=jest.config.js unit/",
    "test:integration": "jest --config=jest.config.js integration/",
    "test:e2e": "jest --config=jest.config.js e2e/"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.1.4",
    "@testing-library/react": "^14.0.0",
    "@testing-library/user-event": "^14.5.1",
    "@types/jest": "^29.5.5",
    "babel-jest": "^29.7.0",
    "identity-obj-proxy": "^3.0.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "mongodb-memory-server": "^9.0.1",
    "supertest": "^6.3.3"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "mongoose": "^7.6.3"
  }
}